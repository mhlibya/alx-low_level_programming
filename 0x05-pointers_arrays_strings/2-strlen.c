#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the lengh of string 17 r
 *
 * @s : variable
 *
 * Return : lengh of string 17 r
 */

int _strlen(char *s)
{
	int r;

	r = strlen(s);
	return (r);
}
