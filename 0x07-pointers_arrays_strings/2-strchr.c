#include "main.h"

/**
 * _strchr - rpifj
 *
 * @s: fkeprof
 * @c: pgrj
 *
 * Return: s;figj
 */

char *_strchr(char *s, char c)
{
char *i;

for (i = s; *i; i++)
{
if (*i == c)
	return (i);
if (i == NULL)
	return (NULL);
else
	return (NULL);
}
return (i);
}
